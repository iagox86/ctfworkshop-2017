apiVersion: v1
kind: Service
metadata:
  name: shortest-service
  labels:
    app: shortest
spec:
  type: LoadBalancer
  ports:
      - port: 8890
        targetPort: 8890
  selector:
    # This should match the app name above
    app: shortest
